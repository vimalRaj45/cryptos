---
description: Start the CryptoSign Digital Signature Demo
---
1. Ensure all dependencies are installed:
// turbo
`pip install -r requirements.txt`

2. Run the Flask application:
`python app.py`

3. Open your browser and navigate to `http://127.0.0.1:5000` to see the full UI/UX demo.
